export const formItemProps = {
    label: String,
    value: String,
    rules: Array,
    required: Boolean,
    disabled: Boolean,
    placeholder: String,
    clearable: Boolean,
    for: String,
}